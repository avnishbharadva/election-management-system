import { Box, styled } from "@mui/material";

export const ModelBox = styled(Box)({
        width: "auto",
        maxWidth: "90vw", 
        backgroundColor: "white",
        borderRadius: "10px",
        overflow: "hidden", 
        maxHeight: "80vh",
        padding: "1rem",
});
      
