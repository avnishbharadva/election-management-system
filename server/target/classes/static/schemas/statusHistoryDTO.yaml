type: object
properties:
  message:
    type: string
    example: "Success"
  data:
    type: array
    items:
      $ref: "#/components/schemas/StatusHistoryDataDTO"

components:
  schemas:
    StatusHistoryDataDTO:
      $ref: "./statusHistoryDataDTO.yaml"